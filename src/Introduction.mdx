import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Introduction" />

<div style={{
  background: 'linear-gradient(135deg, var(--color-brand), var(--color-brand-hover))',
  color: '#fff',
  borderRadius: 'var(--radius)',
  padding: '2.5rem 2rem',
  marginBottom: '2rem',
}}>
  <div style={{ fontSize: '0.8rem', letterSpacing: '0.12em', textTransform: 'uppercase', color: 'rgba(255, 255, 255, 0.85)' }}>
    {`Vue 3 · TypeScript · Tailwind`}
  </div>
  <div style={{ margin: '0.5rem 0 0.75rem', fontSize: '2.4rem', fontWeight: 700, lineHeight: 1.1, color: '#fff' }}>
    {`Component Showcase`}
  </div>
  <div style={{ fontSize: '1.1rem', maxWidth: '46ch', color: 'rgba(255, 255, 255, 0.95)' }}>
    {`A small, clean-room library of presentational Vue components — each built from scratch as a generic, reusable building block and documented here in Storybook.`}
  </div>
  <div style={{ display: 'flex', alignItems: 'center', gap: '0.65rem', marginTop: '1.5rem' }}>
    <img
      src="/avatar.jpg"
      alt="Mihai Bilde"
      style={{ width: '2.75rem', height: '2.75rem', borderRadius: '9999px', objectFit: 'cover', boxShadow: '0 0 0 2px rgba(255, 255, 255, 0.9)' }}
    />
    <div style={{ fontSize: '0.9rem', lineHeight: 1.3, color: 'rgba(255, 255, 255, 0.95)' }}>
      <div style={{ opacity: 0.8 }}>{`Built by`}</div>
      <div style={{ fontWeight: 600 }}>{`Mihai Bilde`}</div>
    </div>
  </div>
</div>

## What this is

This is a personal, portfolio-safe component library. Every component is a **clean,
generic, reusable building block** — written from first principles, with no ties to any
app, brand, or employer codebase. That's what lets each one be previewed, themed, and
dropped into any project in isolation.

Use the sidebar to browse components. Every page is interactive: tweak props live with
the **Controls** panel, read the auto-generated API docs, and check accessibility with
the built-in **a11y** addon.

## Principles

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(220px, 1fr))', gap: '1rem', margin: '1.25rem 0 2rem' }}>
  {[
    ['Props in, UI out', 'Purely presentational. No router, store, or API dependencies — so every component renders in isolation.'],
    ['Variant + size API', 'A consistent variant and size prop pattern across components, backed by typed class maps.'],
    ['Brand-neutral tokens', 'Theming flows through a few generic CSS tokens (--color-brand, --radius). Nothing company-specific.'],
    ['Typed & documented', 'Written in <script setup lang="ts"> with typed props, and auto-documented via Storybook autodocs.'],
  ].map(([title, body]) => (
    <div key={title} style={{
      border: '1px solid #e5e7eb',
      borderRadius: 'var(--radius)',
      padding: '1.1rem 1.2rem',
      background: '#fff',
    }}>
      <h3 style={{ margin: '0 0 0.4rem', fontSize: '1rem', color: 'var(--color-brand)' }}>{title}</h3>
      <p style={{ margin: 0, fontSize: '0.9rem', lineHeight: 1.5, color: '#4b5563' }}>{body}</p>
    </div>
  ))}
</div>

## Components

<ul style={{ lineHeight: 1.9 }}>
  <li><a href="?path=/docs/components-basebutton--docs"><strong>BaseButton</strong></a> — variants, sizes, disabled state, link mode, and icon slots.</li>
  <li><a href="?path=/docs/components-basetag--docs"><strong>BaseTag</strong></a> — status variants, pill shape, and a removable close button.</li>
  <li><a href="?path=/docs/components-baseinput--docs"><strong>BaseInput</strong></a> — labelled text field with sizes, hint/error states, and prefix/suffix slots.</li>
  <li><a href="?path=/docs/components-basemodal--docs"><strong>BaseModal</strong></a> — teleported dialog with backdrop, Escape / scroll-lock handling, and header/footer slots.</li>
  <li><a href="?path=/docs/components-baseselect--docs"><strong>BaseSelect</strong></a> — custom listbox dropdown with full keyboard nav, type-ahead, and hint/error states.</li>
  <li><a href="?path=/docs/components-basemultiselect--docs"><strong>BaseMultiSelect</strong></a> — multi-select listbox with checkboxes, removable chips, and array <code>v-model</code>.</li>
  <li><a href="?path=/docs/components-basetabs--docs"><strong>BaseTabs</strong></a> — accessible tabs (underline / pills) with arrow-key navigation and panel slots.</li>
  <li><a href="?path=/docs/components-basetooltip--docs"><strong>BaseTooltip</strong></a> — hover/focus tooltip with four placements, an arrow, and an optional open delay.</li>
  <li><a href="?path=/docs/components-baseaccordion--docs"><strong>BaseAccordion</strong></a> — collapsible panels (single or multiple open) with animated height and arrow-key navigation.</li>
  <li><a href="?path=/docs/components-baseradiogroup--docs"><strong>BaseRadioGroup</strong></a> — native radio group with custom styling, orientation, sizes, and hint/error states.</li>
  <li><a href="?path=/docs/components-basecheckbox--docs"><strong>BaseCheckbox</strong></a> — single checkbox with an indeterminate (tri-state) mode, sizes, and hint/error states.</li>
  <li><a href="?path=/docs/components-baseswitch--docs"><strong>BaseSwitch</strong></a> — accessible on/off toggle (track + knob) with sizes and hint/error states.</li>
  <li><a href="?path=/docs/components-basebadge--docs"><strong>BaseBadge</strong></a> — compact status/count indicator with subtle/solid styles and an optional status dot.</li>
  <li><a href="?path=/docs/components-baseavatar--docs"><strong>BaseAvatar</strong></a> — image avatar with initials/icon fallback, sizes, shapes, and a presence status dot.</li>
  <li><a href="?path=/docs/components-basespinner--docs"><strong>BaseSpinner</strong></a> — loading spinner with sizes and colors (incl. <code>current</code> to match surrounding text).</li>
  <li><a href="?path=/docs/components-basebreadcrumbs--docs"><strong>BaseBreadcrumbs</strong></a> — semantic breadcrumb trail with a custom separator and middle-collapse for long paths.</li>
  <li><a href="?path=/docs/components-basecard--docs"><strong>BaseCard</strong></a> — slotted container (header/body/footer) with outlined/elevated/filled styles and a centered-icon feature layout.</li>
  <li><a href="?path=/docs/components-basenotification--docs"><strong>BaseNotification</strong></a> — info/success/warning/error alert with an icon, optional title, dismiss, and actions slot.</li>
  <li><a href="?path=/docs/components-baseprogress--docs"><strong>BaseProgress</strong></a> — determinate/indeterminate progress bar with variants, sizes, and an optional value label.</li>
  <li><a href="?path=/docs/components-baseskeleton--docs"><strong>BaseSkeleton</strong></a> — loading placeholder (text/rect/circle) with pulse or shimmer animation.</li>
  <li><a href="?path=/docs/components-basetoast--docs"><strong>BaseToast</strong></a> — teleported toast stack (6 positions) with auto-dismiss and animated enter/leave/move.</li>
  <li><a href="?path=/docs/components-basepagination--docs"><strong>BasePagination</strong></a> — page navigation with prev/next, numbered pages, and ellipsis for long ranges.</li>
  <li><a href="?path=/docs/components-basetextarea--docs"><strong>BaseTextarea</strong></a> — multi-line field with auto-grow, character counter, and hint/error states.</li>
  <li><a href="?path=/docs/components-baseslider--docs"><strong>BaseSlider</strong></a> — range input with a filled track, custom thumb, value label, and sizes.</li>
  <li><a href="?path=/docs/components-basestat--docs"><strong>BaseStat</strong></a> — KPI/metric block with a value, colored trend delta, and optional icon; composes with BaseCard.</li>
  <li><a href="?path=/docs/components-basedivider--docs"><strong>BaseDivider</strong></a> — horizontal/vertical separator with an optional label and solid/dashed styles.</li>
  <li><a href="?path=/docs/components-baselist--docs"><strong>BaseList</strong></a> — list container (plain/divided/bordered) with interactive rows and leading/trailing slots.</li>
  <li><a href="?path=/docs/components-baseemptystate--docs"><strong>BaseEmptyState</strong></a> — centered icon + heading + description + actions for empty screens.</li>
  <li><a href="?path=/docs/components-basedescriptionlist--docs"><strong>BaseDescriptionList</strong></a> — semantic key/value pairs (inline or stacked) with dividers and a custom value slot.</li>
  <li><a href="?path=/docs/components-basekbd--docs"><strong>BaseKbd</strong></a> — inline keyboard-key styling for shortcuts (single key or combo with separator).</li>
  <li><a href="?path=/docs/components-basetimeline--docs"><strong>BaseTimeline</strong></a> — vertical event timeline with a connecting line, colored markers, and custom slots.</li>
  <li><a href="?path=/docs/components-baserating--docs"><strong>BaseRating</strong></a> — star rating (read-only or interactive) with half-steps, keyboard support, and sizes.</li>
  <li><a href="?path=/docs/components-basecode--docs"><strong>BaseCode</strong></a> — inline or block code styling with an optional language label and copy button.</li>
  <li><a href="?path=/docs/components-basesegmentedcontrol--docs"><strong>BaseSegmentedControl</strong></a> — iOS-style single-select toggle group with keyboard navigation.</li>
  <li><a href="?path=/docs/components-basestepper--docs"><strong>BaseStepper</strong></a> — horizontal step indicator for wizards, with optional clickable steps.</li>
  <li><a href="?path=/docs/components-basealertbanner--docs"><strong>BaseAlertBanner</strong></a> — full-width page banner (subtle/solid) with actions and dismiss.</li>
  <li><a href="?path=/docs/components-basepopover--docs"><strong>BasePopover</strong></a> — click-triggered floating panel with placement, click-outside, and Escape.</li>
  <li><a href="?path=/docs/components-basemenu--docs"><strong>BaseMenu</strong></a> — dropdown action menu with keyboard navigation, dividers, and danger items.</li>
  <li><a href="?path=/docs/components-basechipinput--docs"><strong>BaseChipInput</strong></a> — tag/token input; add with Enter/comma, remove with × or Backspace.</li>
  <li><a href="?path=/docs/components-basecalendar--docs"><strong>BaseCalendar</strong></a> — month-grid date picker with min/max, week-start, today highlight, and keyboard nav.</li>
  {/* As new components ship, add an <li> here following the same pattern. */}
</ul>

## Stack

- **Vue 3** with `<script setup lang="ts">` and the Composition API
- **TypeScript** throughout
- **Tailwind CSS v4** (config-less; tokens live in `src/style.css`)
- **Storybook 10** with the docs, a11y, vitest, and chromatic addons
- **Vite** for builds

---

<p style={{ fontSize: '0.85rem', color: '#6b7280' }}>
  Browse the components in the sidebar to see each one in action. New components are added
  one at a time, each as a fresh clean-room build.
</p>
